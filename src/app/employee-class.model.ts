export class EmployeeClass {
    id: string;
    name: string;
    department: string;
    joining_date: string

    constructor(id: string, name: string, department: string, joining_date: string) {
        this.id = id;
        this.name = name;
        this.department = department;
        this.joining_date = joining_date

    }
}
